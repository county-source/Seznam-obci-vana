services:
    - App\Core\RouterFactory::createRouter
    -
        factory: App\Core\Database\DatabaseInitializer(%appDir%/../database/schema.sql)
        setup:
            - initialize

search:
    - in: %appDir%
      classes:
        - *Facade
        - *Factory
        - *Repository
        - *Service
